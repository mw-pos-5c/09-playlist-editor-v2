export default interface Track {
  id: string;
  name: string;
  albumId: string;
  mediaTypeId: string;
  genreId: string;
  composer: string;
  milliseconds: string;
  bytes: string;
  unitPrice: string;
}
